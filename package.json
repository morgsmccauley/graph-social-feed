{
  "name": "near-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ morgsmccauley/near-test",
    "create-local": "graph create --node http://localhost:8020/ morgsmccauley/near-test",
    "remove-local": "graph remove --node http://localhost:8020/ morgsmccauley/near-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 morgsmccauley/near-test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.57.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
